@page "/p/{ProjectId}/tasks"

@* The task projection of the board. A card opens as its own page. *@
<PageTitle>@Loc.Get("TasksNav")</PageTitle>

<BoardView KindFilter="@CardKind.Task" ProjectId="@ProjectId" />

@code {
    [Parameter] public string ProjectId { get; set; } = string.Empty;
}
