@page "/p/{ProjectId}/tasks"
@page "/p/{ProjectId}/tasks/card/{CardId}"

@* The task projection of the board. *@
<PageTitle>@Loc.Get("TasksNav")</PageTitle>

<BoardView Mode="BoardViewMode.Tasks" ProjectId="@ProjectId" CardId="@CardId" />

@code {
    [Parameter] public string ProjectId { get; set; } = string.Empty;
    [Parameter] public string? CardId { get; set; }
}
